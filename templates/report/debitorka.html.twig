{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-12">
            <h1 class="mb-4">Дебиторская задолженность</h1>

            {% if rows is not empty %}
                <div class="table-responsive">
                    <table class="table table-striped table-hover align-middle">
                        <thead>
                            <tr>
                                <th>Оборудование</th>
                                <th>Заявка</th>
                                <th>Заказчик</th>
                                <th class="text-end">₽</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for row in rows %}
                                <tr>
                                    <td>{{ row.equipment }}</td>
                                    <td>{{ row.request }}</td>
                                    <td>{{ row.customer }}</td>
                                    <td class="text-end money">{{ row.sum|number_format(0, ',', ' ') }}</td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            {% else %}
                <div class="alert alert-secondary">Нет данных для отображения.</div>
            {% endif %}

            <a class="btn btn-primary" href="{{ path('report_debitorka', { download: 'csv' }) }}">В Excel</a>

            <hr class="my-4">

            {{ include('report/report_sample.html.twig') }}
        </div>
    </div>
{% endblock %}
